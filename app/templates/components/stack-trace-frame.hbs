<code class="block">{{frame.origFile}}</code>
<ol start="{{start-line frame}}" class="context">
  {{~#each frame.preContext as |line|~}}
    <li>{{line}}</li>
  {{~/each~}}
  <li class="highlight">{{frame.contextLine}}</li>
  {{~#each frame.postContext as |line|~}}
    <li>{{line}}</li>
  {{~/each~}}
</ol>
